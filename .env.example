# Base URL of the BeOnEdge backend API.
# Defaults to http://localhost:4444 if unset (see src/api/client.ts).
#
# When testing on a physical device, use your machine's LAN IP instead of
# localhost, e.g. EXPO_PUBLIC_API_URL=http://192.168.1.10:4444
EXPO_PUBLIC_API_URL=http://localhost:4444
